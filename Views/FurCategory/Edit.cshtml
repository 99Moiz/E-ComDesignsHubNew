@model FursonaHub.API.Models.Category

@{
    
    ViewData["Title"] = "Edit Fursona Category";
}

<div class="row page-header mt-4">
    <div class="col-12">
        <h2 class="fw-bold section-title">Edit <span style="color:var(--brand-lime)">Category</span></h2>
        <p class="text-secondary small">Modify category details and display configurations.</p>
    </div>
</div>

<div class="row">
    <div class="col-md-8 col-lg-6">
        <div class="card app-card p-4">
            <form asp-action="Edit" method="post">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                <input type="hidden" asp-for="Id" />

                <div class="mb-3">
                    <label asp-for="Name" class="form-label text-secondary fw-semibold">Category Name</label>
                    <input asp-for="Name" class="form-control" required autocomplete="off" />
                    <span asp-validation-for="Name" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="SortOrder" class="form-label text-secondary fw-semibold">Sort Order</label>
                    <input asp-for="SortOrder" type="number" class="form-control" required min="0" />
                    <span asp-validation-for="SortOrder" class="text-danger small"></span>
                </div>

                <div class="mb-4 form-check form-switch card p-3 border-secondary" style="background-color: #141414; margin-left: 0;">
                    <div class="d-flex align-items-center justify-content-between">
                        <div>
                            <label class="form-check-label text-white fw-semibold" for="IsActive">Is Active</label>
                            <div class="text-secondary small">Inactive categories won't show in the dropdown options or filters.</div>
                        </div>
                        <input class="form-check-input" type="checkbox" asp-for="IsActive" id="IsActive" />
                    </div>
                </div>

                <div class="d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-outline-light">
                        <i class="bi bi-arrow-left"></i> Cancel
                    </a>
                    <button type="submit" class="btn btn-theme">
                        <i class="bi bi-save"></i> Save Changes
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
